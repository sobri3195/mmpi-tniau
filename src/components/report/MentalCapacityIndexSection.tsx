import type { SummaryAnalysisResult } from '../../types';
import { ScaleBar10 } from './ScaleBar10';

export const MentalCapacityIndexSection = ({ analysis }: { analysis: SummaryAnalysisResult }) => <section className="summary-print-section"><h3 className="text-lg font-black">II. INDEKS KAPASITAS MENTAL</h3><div className="mt-4 overflow-auto"><table className="w-full text-left text-sm"><thead><tr className="border-b"><th className="py-2">Variabel</th><th>Nilai</th><th>Rentang skor</th><th>Makna ringkas</th></tr></thead><tbody>{analysis.mentalCapacityIndex.variables.map((item) => <tr key={item.id} className="border-b border-slate-100 dark:border-slate-800"><td className="py-2 font-semibold">{item.label}</td><td>{item.value ?? 'Belum tersedia karena sumber skala tidak ditemukan.'}</td><td>{item.rangeDescription}</td><td>{item.warning || item.category || 'Belum tersedia'}</td></tr>)}</tbody></table></div><p className="mt-3 text-sm font-bold">Sehingga Indeks Kapasitas Mental responden saat ini adalah {analysis.mentalCapacityIndex.total ?? 'Belum tersedia'}/10.</p><ScaleBar10 value={analysis.mentalCapacityIndex.total} />{analysis.mentalCapacityIndex.warnings?.map((warning) => <p key={warning} className="mt-1 text-xs text-amber-700">⚠ {warning}</p>)}</section>;

import type { SummaryAnalysisResult } from '../../types';
import { ScaleBar10 } from './ScaleBar10';

export const BasicPersonalityIndexSection = ({ analysis }: { analysis: SummaryAnalysisResult }) => <section className="summary-print-section"><h3 className="text-lg font-black">IV. INDEKS KEPRIBADIAN DASAR</h3><div className="mt-4 overflow-auto"><table className="w-full text-left text-sm"><thead><tr className="border-b"><th className="py-2">OCEAN</th><th>Nilai</th><th>Rentang skor</th><th>Kategori</th></tr></thead><tbody>{analysis.basicPersonalityIndex.variables.map((item) => <tr key={item.id} className="border-b border-slate-100 dark:border-slate-800"><td className="py-2 font-semibold">{item.label}{item.englishLabel ? ` (${item.englishLabel})` : ''}</td><td>{item.value ?? 'Belum tersedia'}</td><td>{item.rangeDescription}</td><td>{item.warning || item.category || 'Belum tersedia'}</td></tr>)}</tbody></table></div><p className="mt-3 text-sm font-bold">Sehingga Indeks OCEAN adalah {analysis.basicPersonalityIndex.total ?? 'Belum tersedia'}/10.</p><ScaleBar10 value={analysis.basicPersonalityIndex.total} />{analysis.basicPersonalityIndex.warnings?.map((warning) => <p key={warning} className="mt-1 text-xs text-amber-700">⚠ {warning}</p>)}</section>;

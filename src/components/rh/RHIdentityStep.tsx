import type { RHForm } from '../../types';
import { Field, Input, Select, Textarea } from './common';
export const RHIdentityStep = ({ form, onChange }: { form: RHForm; onChange: (form: RHForm) => void }) => {
  const set = (key: keyof RHForm['identity'], value: string) => onChange({ ...form, identity: { ...form.identity, [key]: value } });
  return <div className="grid gap-4 md:grid-cols-2"><Field label="Nama lengkap"><Input value={form.identity.fullName} onChange={(e) => set('fullName', e.target.value)} /></Field><Field label="Tempat lahir"><Input value={form.identity.birthPlace} onChange={(e) => set('birthPlace', e.target.value)} /></Field><Field label="Tanggal lahir (DD-MM-YYYY)"><Input value={form.identity.birthDateInput} onChange={(e) => set('birthDateInput', e.target.value)} /></Field><Field label="No. telepon"><Input value={form.identity.phone} onChange={(e) => set('phone', e.target.value)} /></Field><Field label="Alamat rumah"><Textarea value={form.identity.homeAddress} onChange={(e) => set('homeAddress', e.target.value)} /></Field><Field label="Agama"><Input value={form.identity.religion} onChange={(e) => set('religion', e.target.value)} /></Field><Field label="Jenis kelamin"><Select value={form.identity.gender} onChange={(e) => set('gender', e.target.value)}><option value="">Pilih</option><option>Laki-laki</option><option>Perempuan</option></Select></Field><Field label="Tujuan pemeriksaan"><Input value={form.identity.examPurpose} onChange={(e) => set('examPurpose', e.target.value)} /></Field></div>;
};

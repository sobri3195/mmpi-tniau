import type { RHForm } from '../../types';
import { Button } from '../ui';
import { RHRedFlagBadge } from './RHRedFlagBadge';
import type { RHValidationResult } from '../../utils/rhValidation';
export const RHReviewSubmitStep = ({ form, validation, onSubmit }: { form: RHForm; validation: RHValidationResult; onSubmit: () => void }) => <div className="space-y-5"><div className="rounded-2xl bg-slate-50 p-4 dark:bg-slate-800"><h3 className="font-black">Ringkasan RH Skrining</h3><div className="mt-3 grid gap-3 md:grid-cols-3"><p><strong>Nama:</strong><br />{form.identity.fullName || form.consent.name}</p><p><strong>No. Test:</strong><br />{form.consent.noTest || '-'}</p><p><strong>Status:</strong><br />{validation.complete ? 'Lengkap' : 'Belum lengkap'}</p></div><div className="mt-3"><RHRedFlagBadge form={form} /></div><p className="mt-3 rounded-2xl border border-amber-200 bg-amber-50 p-3 text-sm font-semibold text-amber-800">Temuan RH memerlukan konfirmasi melalui wawancara klinis oleh profesional berwenang.</p></div><div><h3 className="font-black">Jawaban Ya pada riwayat kesehatan</h3><ul className="mt-2 list-disc pl-5 text-sm leading-6">{form.healthHistory.items.filter((item) => item.answer === 'Ya').map((item) => <li key={item.no}>{item.no}. {item.complaint} — {item.note || '-'}</li>)}{!form.healthHistory.items.some((item) => item.answer === 'Ya') && <li>Tidak ada jawaban Ya.</li>}</ul></div>{validation.missing.length ? <div className="rounded-2xl bg-rose-50 p-4 text-sm text-rose-800"><p className="font-bold">Belum dapat submit. Lengkapi:</p><ul className="mt-2 list-disc pl-5">{validation.missing.slice(0, 20).map((item) => <li key={item}>{item}</li>)}</ul></div> : null}<Button disabled={!validation.complete} onClick={onSubmit}>Submit RH Skrining & Tampilkan Hasil</Button></div>;

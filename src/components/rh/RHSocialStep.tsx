import type { RHForm } from '../../types';
import { Field, Select, Textarea, YesNoRadio } from './common';
export const RHSocialStep = ({ form, onChange }: { form: RHForm; onChange: (form: RHForm) => void }) => {
  const set = (key: keyof RHForm['socialHistory'], value: string) => onChange({ ...form, socialHistory: { ...form.socialHistory, [key]: value } });
  return <div className="grid gap-4 md:grid-cols-2"><Field label="Pernah merasa mempunyai persoalan atau masalah yang dianggap berat?"><YesNoRadio name="serious" value={form.socialHistory.hasSeriousProblem} onChange={(v) => set('hasSeriousProblem', v)} /></Field>{form.socialHistory.hasSeriousProblem === 'Ya' && <><Field label="Tuliskan masalah tersebut"><Textarea value={form.socialHistory.seriousProblemExplanation} onChange={(e) => set('seriousProblemExplanation', e.target.value)} /></Field><Field label="Apakah harus berobat untuk masalah tersebut?"><YesNoRadio name="treatment" value={form.socialHistory.receivedTreatmentForProblem} onChange={(v) => set('receivedTreatmentForProblem', v)} /></Field></>}<Field label="Bagaimana mendapatkan kawan?"><Select value={form.socialHistory.makingFriendsStyle} onChange={(e) => set('makingFriendsStyle', e.target.value)}><option value="">Pilih</option><option>Sukar</option><option>Mudah</option><option>Menunggu disapa lebih dahulu</option><option>Saya biasa memulai percakapan</option></Select></Field><Field label="Kesulitan dengan anggota keluarga termasuk pasangan/anak"><YesNoRadio name="diffFam" value={form.socialHistory.difficultyWithFamily} onChange={(v) => set('difficultyWithFamily', v)} /></Field><Field label="Kesulitan dengan teman sekolah/lingkungan"><YesNoRadio name="diffSchool" value={form.socialHistory.difficultyWithSchoolFriends} onChange={(v) => set('difficultyWithSchoolFriends', v)} /></Field><Field label="Kesulitan dengan orang lain di tempat pekerjaan"><YesNoRadio name="diffWork" value={form.socialHistory.difficultyWithCoworkers} onChange={(v) => set('difficultyWithCoworkers', v)} /></Field><Field label="Bagaimana menghabiskan waktu senggang?"><Textarea value={form.socialHistory.freeTimeActivities} onChange={(e) => set('freeTimeActivities', e.target.value)} /></Field><Field label="Pernah menggunakan/menyalahgunakan narkoba atau zat adiktif lainnya?"><YesNoRadio name="substance" value={form.socialHistory.substanceUseHistory} onChange={(v) => set('substanceUseHistory', v)} /></Field>{form.socialHistory.substanceUseHistory === 'Ya' && <Field label="Keterangan narkoba/zat adiktif"><Textarea value={form.socialHistory.substanceUseExplanation ?? ''} onChange={(e) => set('substanceUseExplanation', e.target.value)} /></Field>}<Field label="Pernah berhadapan dengan masalah hukum?"><YesNoRadio name="legal" value={form.socialHistory.legalProblemHistory} onChange={(v) => set('legalProblemHistory', v)} /></Field>{form.socialHistory.legalProblemHistory === 'Ya' && <Field label="Keterangan masalah hukum"><Textarea value={form.socialHistory.legalProblemExplanation ?? ''} onChange={(e) => set('legalProblemExplanation', e.target.value)} /></Field>}</div>;
};

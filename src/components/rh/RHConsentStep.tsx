import type { RHForm } from '../../types';
import { Field, Input, Textarea } from './common';
export const RHConsentStep = ({ form, onChange }: { form: RHForm; onChange: (form: RHForm) => void }) => {
  const set = (key: keyof RHForm['consent'], value: string | boolean) => onChange({ ...form, consent: { ...form.consent, [key]: value } });
  return <div className="grid gap-4 md:grid-cols-2"><Field label="No. Test"><Input value={form.consent.noTest} onChange={(e) => set('noTest', e.target.value)} /></Field><Field label="Nama"><Input value={form.consent.name} onChange={(e) => set('name', e.target.value)} /></Field><Field label="Tempat / tanggal lahir (DD-MM-YYYY)"><Input placeholder="Jakarta, 01-01-1990" value={form.consent.birthPlaceDate} onChange={(e) => set('birthPlaceDate', e.target.value)} /></Field><Field label="Kesatuan"><Input value={form.consent.unit} onChange={(e) => set('unit', e.target.value)} /></Field><Field label="Alamat rumah"><Textarea value={form.consent.homeAddress} onChange={(e) => set('homeAddress', e.target.value)} /></Field><Field label="No. telepon"><Input value={form.consent.phone} onChange={(e) => set('phone', e.target.value)} /></Field><Field label="Nama institusi pemohon"><Input value={form.consent.institutionRequest} onChange={(e) => set('institutionRequest', e.target.value)} /></Field><Field label="Dibuat di"><Input value={form.consent.createdPlace} onChange={(e) => set('createdPlace', e.target.value)} /></Field><Field label="Pada tanggal"><Input placeholder="DD-MM-YYYY" value={form.consent.createdDate} onChange={(e) => set('createdDate', e.target.value)} /></Field><Field label="Nama yang membuat pernyataan"><Input value={form.consent.signatureName} onChange={(e) => set('signatureName', e.target.value)} /></Field><div className="md:col-span-2 rounded-2xl border border-teal-100 bg-teal-50 p-4 text-sm dark:border-teal-900 dark:bg-teal-950/30"><p>Saya bersedia dilakukan pemeriksaan kesehatan jiwa untuk kepentingan institusi pemohon dan menyatakan seluruh data diisi dengan benar.</p><label className="mt-3 flex items-start gap-2 font-bold"><input type="checkbox" checked={form.consent.statementAccepted} onChange={(e) => set('statementAccepted', e.target.checked)} />Saya menyatakan data yang saya isi benar dan bersedia mengikuti pemeriksaan kesehatan jiwa sesuai ketentuan.</label></div></div>;
};

import type { RHValidationResult } from '../../utils/rhValidation';

const labels: Record<string, string> = { consent: 'Surat Pernyataan', identity: 'Identitas', health: 'Riwayat Kesehatan', education: 'Pendidikan', work: 'Pekerjaan', family: 'Keluarga', social: 'Sosial', review: 'Review' };
export const RHProgress = ({ validation }: { validation: RHValidationResult }) => <div className="rounded-2xl bg-slate-50 p-4 dark:bg-slate-800"><div className="flex items-center justify-between gap-3"><p className="font-black">RH Skrining terisi {validation.percent}%</p><p className="text-xs text-slate-500">Autosave aktif</p></div><div className="mt-3 h-3 rounded-full bg-slate-200 dark:bg-slate-700"><div className="h-3 rounded-full bg-teal-600" style={{ width: `${validation.percent}%` }} /></div>{validation.missing.length ? <div className="mt-3 text-sm"><p className="font-bold text-amber-700">Bagian belum lengkap:</p><ul className="mt-1 list-disc pl-5">{Object.entries(validation.sectionMissing).filter(([, items]) => items.length).map(([section, items]) => <li key={section}>{labels[section]} ({items.length})</li>)}</ul></div> : <p className="mt-3 text-sm font-semibold text-teal-700">Semua bagian lengkap dan siap submit.</p>}</div>;
